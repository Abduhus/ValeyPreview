# ValleyPreview Environment Configuration
# Copy this file to .env and update the values

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration (Optional - will use in-memory storage if not provided)
# For Neon Database (recommended for production)
# DATABASE_URL=postgresql://username:password@hostname:port/database?sslmode=require

# For local PostgreSQL
# DATABASE_URL=postgresql://localhost:5432/valley_preview

# CORS Configuration (Optional - for production)
# FRONTEND_URL=https://your-domain.com

# Example production configuration:
# NODE_ENV=production
# PORT=10000
# DATABASE_URL=postgresql://user:pass@db.example.com:5432/valley_preview?sslmode=require
# FRONTEND_URL=https://valley-preview.example.com